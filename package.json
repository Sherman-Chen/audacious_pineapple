{
  "name": "filter",
  "version": "1.0.0",
  "description": "basic image filtering app",
  "main": "index.js",
  "scripts": {
    "deploy": "git push origin master; firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sherman Chen",
  "license": "ISC"
}
